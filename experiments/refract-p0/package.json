{
  "name": "refract-p0",
  "version": "0.3.0",
  "private": true,
  "description": "Minimum runnable REFRACT-P0 specimen with one canonical historical source and zero runtime dependencies.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "demo": "node src/demo.js",
    "falsify": "node src/falsification-demo.js"
  },
  "engines": {
    "node": ">=20"
  }
}
