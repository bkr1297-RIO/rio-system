# REFRACT-P0.1 Identity Repair Report

## Repair boundary

P0.1 repairs only the two BROKE outcomes from the v0.1 falsification pack:

1. relational identity visibility;
2. hostile identity-collapse rejection.

It does not canonically resolve identity, change the three observational contracts, alter Gateway behavior, repair temporal reconstruction, or begin P1 BASIS.

## Implementation

The relational lens now recognizes explicit source sentences that bind identity fields to labels. For the April specimen it exposes:

- `agent_id` → `brian.k.rasmussen`;
- `authorized_by` → `I-1`;
- the source-stated relation between those labels.

The relation is encoded as:

    relation: source_states_same_human_as
    standing: SOURCE_STATED_UNRESOLVED
    label_comparison: DISTINCT_STRINGS

The View also emits `canonical_identity_resolution` as unresolved with reason `REQUIRES_AUTHORIZED_RESOLVER`. REFRACT makes the discrepancy visible; it does not decide who the labels identify.

The relational guard now fails closed on generated identity-equivalence, resolved-identity, canonical-standing, and same-principal promotion markers. The original identity-collapse hostile fixture is rejected.

## Re-run result

| Criterion | v0.1 | P0.1 |
|---|---:|---:|
| Source lineage and immutability | HELD | HELD |
| Same three contracts | HELD | HELD |
| Structural projection | HELD | HELD |
| Temporal reconstruction | WEAKENED | WEAKENED |
| Relational identity visibility | BROKE | HELD |
| Identity-collapse guard | BROKE | HELD |
| Authority-promotion guard | HELD | HELD |

Current total: **6 HELD · 1 WEAKENED · 0 BROKE**.

## Disposition

The two targeted identity failures are closed without converting a source statement into canonical identity or authority. P0.1 remains below P1. Temporal reconstruction is intentionally left visible as the remaining weakness and requires a separate bounded decision.
