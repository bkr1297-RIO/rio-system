{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pgtc.rio.dev/schemas/tes-config.schema.json",
  "title": "PGTC Trajectory Envelope State (TES) Configuration",
  "description": "Constraints on allowed paths within a trajectory. Defines permitted action classes, valid state transitions, and allowed resource scopes.",
  "type": "object",
  "properties": {
    "allowed_actions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Action classes permitted in this trajectory"
    },
    "blocked_actions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Action classes explicitly blocked in this trajectory"
    },
    "valid_transitions": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string" }
      },
      "description": "Map of current_state → [allowed_next_states]"
    },
    "current_state": {
      "type": "string",
      "description": "Current state in the trajectory"
    },
    "allowed_scopes": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Resource scopes permitted in this trajectory"
    },
    "x_extensions": {
      "type": "object",
      "description": "Implementation-specific extension fields"
    }
  },
  "additionalProperties": false
}
